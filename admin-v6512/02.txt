async function saveGenerator(ev){ev.preventDefault();const id=el('generatorId').value||null,payload={product_id:el('generatorProduct').value,kva:Number(el('generatorKva').value),kw:Number(el('generatorKw').value),voltage_support:el('generatorVoltage').value,fuel:el('generatorFuel').value.trim(),sort_order:Number(el('generatorSort').value)||100,active:el('generatorActive').checked};if(!payload.product_id||payload.kva<=0||payload.kw<=0)return show('Bitte Generator-Leistungsdaten prüfen.',true);const q=id?db.from('generator_rental_links').update(payload).eq('id',id):db.from('generator_rental_links').insert(payload);const {error}=await q;if(error)return show(error.message,true);el('generatorDialog').close();show('Mietgenerator-Zuordnung gespeichert.',false,true);await loadAll()}
window.deleteDevice=async id=>{if(!confirm('Gerät wirklich löschen? Deaktivieren ist oft die bessere Wahl.'))return;const {error}=await db.from('generator_devices').delete().eq('id',id);if(error)return show(error.message,true);show('Gerät gelöscht.',false,true);await loadAll()};window.deleteGenerator=async id=>{if(!confirm('Zuordnung wirklich löschen?'))return;const {error}=await db.from('generator_rental_links').delete().eq('id',id);if(error)return show(error.message,true);show('Zuordnung gelöscht.',false,true);await loadAll()};
el('openRentalAdminBtn').addEventListener('click',openRentalAdmin);el('checkSessionBtn').addEventListener('click',checkSession);el('logoutBtn').addEventListener('click',logout);el('refreshBtn').addEventListener('click',loadAll);el('newCategoryBtn').addEventListener('click',()=>openCategory());el('newDeviceBtn').addEventListener('click',()=>openDevice());el('newApplicationBtn').addEventListener('click',()=>openApplication());el('applicationForm').addEventListener('submit',saveApplication);el('newGeneratorBtn').addEventListener('click',()=>openGenerator());el('categoryForm').addEventListener('submit',saveCategory);el('deviceForm').addEventListener('submit',saveDevice);el('generatorForm').addEventListener('submit',saveGenerator);
(async()=>{if(isDemo)return show('Supabase ist nicht konfiguriert.',true);const {data}=await db.auth.getSession();if(data.session)await showAdmin()})();
